import css from 'styled-jsx/css'

const style = css`
  .logo{
    :global(.avc-logo){
      width: 2.8rem;
      height: 2.8rem;
    }
  }
  .content{
    content-visibility: auto;
  }
`

export default style